# Copy this file to `.env` for local dev and to Netlify Site settings > Environment variables for production.
# Frontend will use this as API base when making requests. Example values:
# - Local dev backend: http://localhost:5000
# - Deployed backend (Railway/Render/Vercel/EC2/etc): https://your-backend.example.com
VITE_API_BASE=
